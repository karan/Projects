#include <iostream>
#include <fstream>

#define MAX_N 20

int cows[MAX_N];
bool bcows[MAX_N];

int main() {

	for (int i = 0; i < MAX_N; i++) bcows[i] = true;

	ifstream fin;
	fin.open("escape.in");
	int numcows;

}
